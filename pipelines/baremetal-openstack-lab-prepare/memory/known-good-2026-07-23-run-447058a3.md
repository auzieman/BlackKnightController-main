# 10 VIDEO known-good proof — Server1 Trixie PXE

Run: 447058a3-52df-4baa-a325-6254798b197e
Date: 2026-07-23
Target: r630-openstack-01 / Server1
BMC: 10.20.0.119
PXE/install IP: 10.20.0.240
PXE MAC: 80:18:44:de:fd:38

## Result

Complete. Server1 destructively installed Debian Trixie from one-shot PXE, disarmed PXE after preseed handoff, booted from local disk, and enrolled over SSH as root.

## Timing evidence

- 07:11:39 — iPXE fetched /pxe/debian-trixie.ipxe, initrd.gz, and linux from ns1 HTTP.
- 07:12:09 — Debian Installer fetched /pxe/preseed/r630-openstack-01-trixie.cfg over HTTP 200.
- 07:17:37 — Fresh post-install DHCP lease for 10.20.0.240.
- 07:18:59 — SSH proof captured from installed OS.

## Installed OS proof

```text
hostname: r630-openstack-01
address: eno1 10.20.0.240/24
install disk: /dev/sda PERC H730P Mini 3.3T
partitions:
  /dev/sda1 976M vfat EFI
  /dev/sda2 3.2T ext4 root
  /dev/sda3 63.9G swap
BKC receipt: /var/lib/bkc/base-provisioning.json
EFI boot: Boot0004* debian, BootCurrent 0004
```

## Protected installer contract

This pipeline intentionally matches `pipelines/ns1-trixie-pxe-smoke` mechanics:

- no `console=` arguments in iPXE;
- `auto=true priority=critical debconf/priority=critical`;
- `preseed/url=` and `url=` both present;
- `partman-auto/method string regular`;
- `partman-auto/choose_recipe select atomic`;
- `partman-partitioning/confirm_write_new_label boolean true`;
- `tasksel tasksel/first multiselect standard, ssh-server`;
- `grub-installer/bootdev string /dev/sda`;
- no `method efi`, no `force-efi-extra-removable`, no serial-console detours unless explicitly debugging.

## Do not regress

If this lane fails later, compare against `ns1-trixie-pxe-smoke` before changing installer semantics. Do not reintroduce serial/VGA console experiments, all-disk wipe loops, alternate partman methods, or hand-written grub logic without a new proven trace.
