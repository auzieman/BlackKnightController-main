{
  "id": "delete-namespace",
  "kind": "command",
  "summary": "Remove the rx-demo namespace if any dependent resources remain.",
  "command": "k3s kubectl delete namespace rx-demo --ignore-not-found=true --timeout=240s"
}
