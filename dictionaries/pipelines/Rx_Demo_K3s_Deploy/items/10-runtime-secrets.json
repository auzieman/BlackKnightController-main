{
  "id": "runtime-secrets",
  "kind": "check",
  "summary": "Confirm the runtime secret exists without exposing secret values.",
  "command": "kubectl -n rx-demo get secret rx-demo-secrets",
  "fallback": "Create rx-demo-secrets outside the repo with local demo-only SQL Server and RabbitMQ credentials."
}

