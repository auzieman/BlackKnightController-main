{
  "id": "runtime-secrets",
  "kind": "secret-handling",
  "summary": "Ensure the runtime secret exists with demo-only generated values without exposing secret contents.",
  "command": "k3s kubectl -n rx-demo create secret generic rx-demo-secrets --dry-run=client -o yaml | k3s kubectl apply -f -",
  "fallback": "If secret creation fails, fix the BKC runtime-secrets stage and rerun the pipeline rather than applying secrets manually."
}
