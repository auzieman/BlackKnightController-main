{
  "id": "visible-change-check",
  "kind": "validation",
  "summary": "Generate visible UI/API activity after redeploy.",
  "transport": "bkc-ssh",
  "command": "curl rx-ui lookup/approve/refill routes through the discovered NodePort endpoint",
  "expected": "UI smoke output includes the active rx-ui NodePort URL."
}
