# Trixie Workstation Personalize Dictionary

Runtime values for the VMID 132 post-install Trixie workstation profile.

Keep RustDesk disabled until `rustdesk_deb_url` is set to a verified package
artifact for the desired release.
