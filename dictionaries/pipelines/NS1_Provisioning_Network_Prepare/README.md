# NS1 Provisioning Network Prepare

Runtime dictionary for preparing ns1 with an isolated provisioning network.

This folder is lab-local runtime state. The reusable recipe lives in
`pipelines/ns1-provisioning-network-prepare/`.

Do not enable DHCP or PXE from this lane. This only prepares and validates the
network interface.

