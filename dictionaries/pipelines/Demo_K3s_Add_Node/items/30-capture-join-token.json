{
  "id": "capture-join-token",
  "kind": "secret-handling",
  "summary": "Capture the k3s join token from the existing server without printing it in the recording.",
  "command": "ssh <k3s-server> 'sudo cat /var/lib/rancher/k3s/server/node-token' > /tmp/k3s-node-token",
  "safety": "Do not show token contents on camera. Prefer BKC secret storage or an ephemeral file with restrictive permissions."
}

