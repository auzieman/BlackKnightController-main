# Windows 10 Workstation Personalize Dictionary

Runtime values for the VMID 136 post-install Windows workstation profile.

Set `target_host` to the current Windows management IP or DNS name before
running if `win10-pxe-136.lab.auzietek.com` is not registered yet.
