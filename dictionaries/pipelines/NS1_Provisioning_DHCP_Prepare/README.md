# NS1 Provisioning DHCP Prepare

Lab-local dictionary and review items for preparing DHCP on ns1's isolated
provisioning interface.

The current lab values keep DHCP disabled. Enabling the service should be a
separate explicit run after config rendering and syntax validation are visible
in BKC.
